package com.moo.ttp.experiment;


/*

public class StatisticalValues {

	public double min = Double.MAX_VALUE;
	public double max = Double.MIN_VALUE;
	public double mean;
	
	public String name;
	
	
	public StatisticalValues(String name, double[] values) {
		mean = Arrays.stream(values).average().getAsDouble();
		
		for(double v : values) {
			if (v > max) max = v;
			if (v < min) min = v;
		}
		
	}
	
	
	@Override public String toString() {
	    StringBuilder result = new StringBuilder();
	    result.append("min: " + min + '\n');
	    result.append("max: " + max+ '\n');
	    result.append("mean: " + mean+ '\n');
	    return result.toString();
	  }

}
	*/
	

